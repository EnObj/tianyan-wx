<view class="padding-box"> 
  <textarea style="width:100%;" show-confirm-bar bindconfirm="finished" placeholder="请输入内容" bindinput="contentChange" value="{{init}}" focus maxlength="{{limit}}"></textarea> 
</view> 
 
<view class="menu"> 
  <button bindtap="confirm" type="primary">完成</button> 
</view>